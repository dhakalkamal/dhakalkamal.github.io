# CLAUDE.md

Claude Code-specific instructions only.

This file is the Claude Code entry point for this repository.
Read `PROJECT.md` first, then follow it as the shared project context.

Claude Code must not modify `AGENTS.md` unless explicitly requested.
Use `.claude/settings.json` only for Claude Code local read access.

Prefer minimal diffs over broad rewrites.
